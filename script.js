let button = document.querySelector("#coin");
button.addEventListener("mouseup", (e) => {
    let addBalance = balance.innerText++;
    switch (e.button) {
        case 0:
            addBalance;
    }
});